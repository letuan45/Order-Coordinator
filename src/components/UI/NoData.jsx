import React from "react";
import { Typography } from "@material-tailwind/react";

const NoData = () => {
  return <Typography className="text-center">Không có dữ liệu nào</Typography>;
};

export default NoData;
